# BBDD - Usuarios

Ejecuta `schema.sql` antes del esquema de `lista-pendientes`. Define:

- `workspaces`
- `workspace_members`
- politicas RLS para miembros, invitaciones y administradores

Roles disponibles: `owner`, `admin`, `editor` y `viewer`.
